name: ENV_MENTORIA
channels:
  - conda-forge
dependencies:
  # — Núcleo —
  - python=3.10
  - pip
  - numpy>=1.24
  - pandas>=2.0

  # — Modelagem —
  - scikit-learn>=1.4
  - xgboost>=2.0
  - optuna>=3.6

  # — Visualização & explainability —
  - matplotlib>=3.8
  - seaborn>=0.13
  - shap>=0.45

  # — Notebook & utilidades —
  - jupyterlab
  - openpyxl          # leitura/escrita de arquivos .xlsx

  # — Extras via pip (ainda sem receita no conda) —
  - pip:
      - optbinning>=0.18          # OptimalBinning
      - category_encoders>=2.6    # Target / Leave-One-Out / etc.
